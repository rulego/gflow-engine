# 贡献指南

感谢关注 GFlow Engine。本指南说明提交贡献前需要知道的事：开发环境、代码与提交规范、测试要求，以及 AI 辅助贡献的边界。

## 项目性质

GFlow Engine 是**以 Go 库形式被宿主应用嵌入**的审批工作流引擎：不含 UI、不提供 HTTP 服务，导出的 API 与错误哨兵就是对外契约。这决定了本仓库的两条底线：

- **兼容性敏感**：任何导出 API 的签名、语义、错误分类变化都是破坏性变更，须在 PR 描述中显式声明，并写清宿主应用的适配点（宿主靠 `errors.Is` 把哨兵错误映射为 HTTP 状态码）。
- **鉴权是引擎职责**：多租户隔离与操作人校验在引擎层收口，不依赖宿主自觉。新增任何变更类操作，必须自带「操作人 + 租户」校验。

## 交流语言

Issue、PR、commit、代码注释、docs/ 目录文档均使用中文；代码标识符、错误消息使用英文。README.md（英文）与 README_ZH.md（中文）成对维护，改动需同步。

## 开发环境

- Go 1.24+，依赖 RuleGo v0.37+
- 单元测试默认跑 SQLite 内存库，无需预建环境；涉及行锁序列化的用例只有在真实 PostgreSQL / MySQL 上才有意义（SQLite 会静默忽略 `FOR UPDATE`），CI 会在真库上单独跑 `service/instance_lock_test.go`
- 建库脚本：`scripts/00.init_bpm_pg.sql` / `scripts/00.init_bpm_mysql.sql`
- 集成示例见 `examples/`

## 本地验证（提交前必做）

```bash
gofmt -l .          # CI 有格式门禁，不干净直接挂
go vet ./...
go build ./...
go test ./...
```

CI 等价于 `go test -race -count=1 -timeout 20m ./...` 加真库锁测试。

## 代码规范

以下规范来自本仓库已在使用的模式，新代码请对齐最近似文件的写法：

**错误处理**

- 用包级哨兵错误（`ErrNotFound` / `ErrPermissionDenied` / `ErrValidation` / `ErrConflict` / `ErrInstanceTerminal` 等），一律 `fmt.Errorf("...: %w", err)` 包装，保持 `errors.Is` 链路完整。
- 跨租户访问按 `ErrNotFound` 伪装，与其他操作口径一致——不向调用方泄露资源存在性。
- fail-closed 优先。确需降级放行（fail-open）的地方，必须注释说明为什么放行是安全的。

**并发与事务**

- 任务/实例的一切变更走 `WithInstanceTx`（实例行锁），与并发 Complete 串行化；禁止绕过锁直改。
- 锁内只使用 `scope` 提供的 tx-bound DAO；经由全局连接的查询在 SQLite 等单写锁数据库上会与外层事务互等死锁。
- 副作用（事件派发、通知）挂 `scope.AfterCommit`，不落在事务内。
- 锁外做的廉价鉴权，若存在并发改派窗口，在锁内对最新快照复跑。

**数据访问**

- 运行时表 / 历史表（`wf_*` / `wf_hi_*`）口径分离，改动时两侧同步考虑。
- gorm `Updates(struct)` 忽略零值：清空 assignee/owner 等字段须用空串指针或 `Select` 强制写入，防残留。
- 新增列须同步三处 DDL：`scripts/00.init_bpm_pg.sql`、`scripts/00.init_bpm_mysql.sql` 与 e2e 建表语句，列序保持一致。

**注释**

- 中文，只写代码看不出来的约束：锁语义、跨库兼容、安全取舍、口径对齐的出处。历史与修复过程进 commit message，产品口径进 docs/。
- 不引入与本项目无关的第三方项目名。

AI 味注释的典型形态，评审直接打回：

- **复述代码**：循环上方写「遍历子任务并匹配 assignee」——代码本身已经说明了，零信息。
- **修复叙事**：「此前 X 漏校验导致越权，现改为 Y」——这是写给评审看的变更说明，合并后即成噪音。约束要用恒真句式写给下一个读代码的人：「跨租户按 ErrNotFound 伪装，与 Claim 同口径」；「此前如何」放进 commit message。
- **空洞填充**：无下文的 `// TODO: 优化`、`// 注意：此处可能失败`、复述签名的 doc comment（`// GetUserByID 根据ID获取用户`）。
- **教学式**：科普语言或数据库常识（context 的用途、`FOR UPDATE` 的含义）。读者是引擎开发者，不是教程受众。
- **表演式安全声明**：「此处已确保不可伪造 / 不可越权」——安全结论必须能指出相邻代码里的执行点，指不出来就是误导。现成反例：「字段无 json tag 故无法反序列化伪造」——`encoding/json` 对无标签导出字段按字段名大小写不敏感匹配，该结论不成立。

## 测试要求

- 新功能与缺陷修复必须带用例；安全类修复的用例要能证明「修复前可复现漏洞、修复后拒绝」，并覆盖拒绝路径的错误哨兵（`errors.Is` 断言）。
- 优先复用既有测试基建（内存库 fixture、引擎替身），不要为单个用例自起一套。
- 期望值来自审批语义与需求，不要从实现反推——「实现是什么测试就断言什么」的用例没有价值。
- 涉及并发竞争的修复，说明为什么用例能约束住该场景；纯靠时序的断言写不进去就别写，改为对不变量的断言。

**测试文件组织**

- 默认与源文件一一对应：`task_service_sign.go` 的单测进 `task_service_sign_test.go`，按源文件找用例是大多数场景。
- 跨源文件的不变量（实例行锁、租户隔离、鉴权 fail-closed这类贯穿多个文件的行为）单独建主题测试文件，按行为命名（如 `instance_lock_test.go`）。同一行为的用例集中在一个文件，不要拆散进各源文件对应的测试文件里。
- 共享 fixture 与测试替身集中在 `testutil_test.go` 这类公共文件，各测试文件只引用不复制。
- 不要把巨型测试文件当堆放场：与 `*_impl_test.go` 主题无关的用例不该继续往里追加。

## 提交与 PR

**Commit**：`feat:` / `fix:` / `test:` / `docs:` / `refactor:` 前缀 + 中文单行，说清「改了什么、为什么」，不带签名、不带 AI 署名尾注。未推送前可直接 `--amend`。

**PR**：一个 PR 一件事。开 PR 前 rebase 到 `origin/main`，分支从 main 顶端切出，不要堆叠在未合入的其他 PR 之上——否则合并后版本树会出现嵌套与交叉的分叉。描述按现有 PR 的成熟格式组织：

1. 背景——发现什么问题，影响面是什么；
2. 改动——逐条列出，写清楚行为变化与取舍（尤其鉴权收紧、可用性回退类）；
3. 破坏性变更（如有）——宿主需要做什么；
4. 测试——新增哪些用例、如何验证；
5. 验证——本地四件套的实际结果。

鉴权/安全类改动建议在动手前先开 issue 对齐方案，避免与维护者的口径冲突导致返工。

**文档**：用户可感知的变化（DSL 字段、事件、API、行为）同步更新 `docs/` 对应文档与 `docs/CHANGELOG.md`；README 双语同步。

**安全漏洞**：请勿以公开 issue/PR 提交含利用细节的报告，优先通过 GitHub 私密安全通告联系维护者。

## AI 辅助贡献规范

本仓库接受 AI 辅助的开发与安全审计，已有实践证明其价值；但 AI 生成代码的失效模式有自己的特点，作如下要求：

**披露（建议，非强制）**

AI 辅助的 PR 建议在描述开头用一行注明工具与 AI 参与程度，例如：

> 🤖 本 PR 由 <工具 / 模型> 辅助生成，参与 <全部变更 / 部分 / 仅审计>，已人工审核。

维护者据此校准审查深度。不设固定模板，未注明的 PR 照常进入审查——但审查标准不因声明与否而改变，下述责任与检查点对所有贡献一视同仁。

**责任**

提交人对 PR 的全部变更负全责。「AI 写的、我没细看」不是缺陷的免责理由。人工审核的最低标准：读懂每一处控制流变化，对鉴权、事务边界、锁语义逐行确认；PR 描述里的每一条声称（含「go test 全绿」）亲自复跑核对。

**AI 生成代码的额外检查点**

AI 辅助的贡献在合并审查时按上述通用规范执行，另加三条本仓库实测踩过的坑：

1. **警惕安全收紧带来的可用性回归。** AI 擅长枚举边界漏洞，但 fail-closed 类改动容易误伤合法流程（如：定义解析失败即拒绝动作，会让设计器改坏节点后在途实例的操作全部被卡）。每处「从放行改为拒绝」都必须评估正常业务的受影响面，并在 PR 描述里写明取舍。
2. **核对描述与注释里的每一条安全论断。** AI（和人类）都容易写下「看起来对」的断言。例：无 json tag 的导出字段照样会被 `encoding/json` 按字段名大小写不敏感匹配反序列化——「没有 tag 所以无法从请求体伪造」这类论断是错的。安全结论要有依据，不要凭直觉陈述。
3. **范围最小化。** AI 常顺手重构无关代码、补空洞的防御性检查、给每行代码加解说注释。diff 必须限定在声明的改动范围内，无关改动拆出去。

**AI 生成的测试**

按「测试要求」一节执行：期望值来自语义而非实现；拒绝路径与放行路径都要覆盖；不允许出现断言恒真、复述实现的用例。
